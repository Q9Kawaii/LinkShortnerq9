rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // This allows anyone with the link to read/write for now
      allow read, write: if true; 
    }
  }
}